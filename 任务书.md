# 拆补法时家奇门起盘程序任务书

## 1. 项目概述

- **项目名称：** 拆补法时家奇门起盘程序
- **文档版本：** v0.1
- **编制日期：** 2026-09-08
- **项目目标：** 开发一个输入公历时间与地点后，按照统一、可追溯的拆补法规则自动完成时家奇门排盘，并输出干支、阴阳遁、局数、九宫地盘、天盘、九星、八门、八神、值符、值使、旬空及驿马等信息。

首版以“排盘正确、规则透明、结果可核验”为核心，不提供未经规则验证的自动吉凶断语。

## 2. 建设范围

### 2.1 首版功能

1. 支持手动输入公历日期、时间，也支持按当前时间一键起盘。
2. 接收时区和经纬度；当前时间模式默认使用设备当前时刻。
3. 计算农历日期、四柱干支和二十四节气交接时刻。
4. 支持北京时间和真太阳时两种时间模式。
5. 判定阴遁、阳遁及上元、中元、下元。
6. 按拆补法确定局数。
7. 排布地盘三奇六仪。
8. 排布天盘三奇六仪和九星。
9. 排布八门与八神。
10. 计算旬首、值符、值使、旬空和驿马。
11. 在九宫盘前显示旬首、值符、值使、旬空地支及马星；在对应宫位标注“空亡”和“马星”。
11. 输出文本九宫盘及结构化 JSON 数据。
12. 显示关键计算过程和所用规则版本，支持人工复核。
13. 为后续网页界面、图片导出和断盘模块保留接口。

### 2.1.1 当前时间一键起盘

程序必须提供“按当前时间起盘”功能，适用于命令行、API 和网页界面：

- 未提供 `datetime` 时，自动读取运行设备的当前时间。
- 将当前时间转换为用户选择的时区；默认使用 `Asia/Shanghai`（UTC+8）。
- 记录实际取时瞬间，精确到秒，并在结果中显示排盘时间。
- 使用当前时间对应的经度、纬度计算标准时间或真太阳时。
- 在节气交接、子初换日和时辰交界附近，保留秒级时间并明确采用的边界规则。
- 一键起盘应等价于将同一时刻显式填入 `datetime` 后起盘。
- 用户可以点击“重新起盘”刷新当前时间；程序不应在后台自动改变已经生成的盘。
- 允许用户锁定当前时间后复制结果，避免查看过程中时间变化造成结果不一致。

### 2.2 首版不包含

- 用神自动选取。
- 格局、旺衰和吉凶的自动判断。
- 六亲及门派专属断法。
- 自动生成预测结论。
- 用户账户、云端同步和商业支付功能。

## 3. 规则基线

不同奇门流派对拆补法、换日、中宫寄宫和值使起法存在差异。程序应将这些差异放入独立规则配置，计算结果必须标明当前采用的规则版本。

### 3.1 时间与历法

- 输入使用带时区的公历时间。
- 默认时区为 `Asia/Shanghai`（UTC+8）。
- 默认使用标准时间，可选真太阳时。
- 节气采用定气法，交节时刻至少精确到分钟。
- 经度默认值为东经 120 度，用户可修改。
- 日柱换日规则必须配置化，至少支持午夜换日与子初换日。
- 节气边界以精确交节时刻为准，交节前后分别采用对应节气规则。

### 3.2 阴遁与阳遁

- 冬至交节后至夏至交节前采用阳遁。
- 夏至交节后至冬至交节前采用阴遁。
- 切换边界采用节气的实际交接时刻。

### 3.3 拆补法与三元

- 每个节气分上元、中元、下元，每元原则上五日。
- 交节发生在一日之中时，交节前归前一节气，交节后归当前节气。
- 三元判定、符头定位和不足日数补足规则独立封装为 `YuanStrategy`。
- 默认拆补法的具体口径须通过权威规则资料和标准排盘案例确认后锁定。
- 严禁在同一张盘中混用拆补法、置闰法或其他定局方法。

### 3.4 九宫体系

采用洛书九宫：

```text
巽四  离九  坤二
震三  中五  兑七
艮八  坎一  乾六
```

宫位基础数据：

| 宫号 | 宫名 | 方位 | 五行 |
| --- | --- | --- | --- |
| 1 | 坎 | 北 | 水 |
| 2 | 坤 | 西南 | 土 |
| 3 | 震 | 东 | 木 |
| 4 | 巽 | 东南 | 木 |
| 5 | 中 | 中央 | 土 |
| 6 | 乾 | 西北 | 金 |
| 7 | 兑 | 西 | 金 |
| 8 | 艮 | 东北 | 土 |
| 9 | 离 | 南 | 火 |

### 3.5 地盘三奇六仪

地盘排布次序：

```text
戊、己、庚、辛、壬、癸、丁、丙、乙
```

- 戊从局数对应宫位起排。
- 阳遁顺布九宫，阴遁逆布九宫。
- 中五宫及寄宫方式必须配置化，并在输出中标明。

### 3.6 六甲旬首

| 六甲旬 | 旬首遁仪 |
| --- | --- |
| 甲子旬 | 戊 |
| 甲戌旬 | 己 |
| 甲申旬 | 庚 |
| 甲午旬 | 辛 |
| 甲辰旬 | 壬 |
| 甲寅旬 | 癸 |

程序根据时柱定位所属旬和旬首遁仪，再在地盘中定位该遁仪所在宫。

### 3.7 天盘、九星、八门与八神

- 值符星由旬首遁仪所在宫的九星确定。
- 值使门由旬首遁仪所在宫的八门确定。
- 值符随时干落宫，并据此旋转天盘九星与天盘干。
- 值使随时支运行，移动起点和计数方式由已确认的拆补法规则决定。
- 阳遁与阴遁的顺逆规则必须分别测试。
- 八门次序采用：休、生、伤、杜、景、死、惊、开。
- 八神次序采用：值符、腾蛇、太阴、六合、白虎、玄武、九地、九天。
- 中宫寄宫、门星入中和阴阳遁布神细则必须写入规则配置。

### 3.8 旬空与驿马

- 输出当前时柱所属旬及旬空地支。
- 将旬空地支映射到对应宫位，并标记空亡。
- 旬空必须同时出现在结构化结果的 `xun.void_branches`、宫位的 `void_branches` 及文本盘面。
- 按日支或时支所采用的规则计算驿马；依据字段与算法必须明确。
- 马星默认按时支计算，输出马星地支、落宫、取值依据，并在宫位中标记 `horse: true`。
- 辅助信息单独计算，不应反向影响主盘排布。

## 4. 待确认的门派口径

编码核心排盘引擎前，应通过规则资料及标准案例锁定以下内容：

1. 拆补法三元划分与符头定位的具体算法。
2. 节气交接当日的拆分、补足及跨节处理方法。
3. 日柱采用午夜换日还是子初换日。
4. 中五宫的天盘干、九星、八门与八神寄宫规则。
5. 值使随时支运行的起点、顺逆及计数方式。
6. 天禽星与天芮星同宫时的显示和旋转方式。
7. 驿马按日支还是时支计算。
8. 真太阳时是否同时用于节气边界、日柱和时柱判定。

以上规则应形成独立的《规则确认表》，每条记录资料来源、采用口径、版本和验证案例。

## 5. 功能需求

### 5.1 输入

```json
{
  "datetime": "2026-09-08T14:30:00+08:00",
  "use_current_time": false,
  "timezone": "Asia/Shanghai",
  "longitude": 120.0,
  "latitude": 30.0,
  "time_mode": "standard_time",
  "yuan_strategy": "chai_bu_v1",
  "day_boundary": "midnight"
}
```

### 5.2 输出

```json
{
  "input": {},
  "calendar": {
    "solar_datetime": "",
    "lunar_date": "",
    "year_pillar": "",
    "month_pillar": "",
    "day_pillar": "",
    "hour_pillar": ""
  },
  "solar_term": {
    "name": "",
    "transition_time": "",
    "dun": "yang",
    "yuan": "upper",
    "ju": 1
  },
  "xun": {
    "head": "",
    "hidden_stem": "",
    "void_branches": []
  },
  "horse": {
    "basis": "hour",
    "basis_branch": "",
    "branch": "",
    "palace": 0
  },
  "chief": {
    "zhi_fu": "",
    "zhi_shi": ""
  },
  "palaces": [],
  "rule_version": "chai_bu_v1"
}
```

每个宫位至少包含：

- 宫号、宫名、方位和五行。
- 地盘干、天盘干。
- 九星、八门、八神。
- 值符和值使标记。
- 旬空和驿马标记。
- 旬空地支列表、空亡宫位标记、马星地支及马星宫位标记。
- 中宫寄宫信息。

### 5.3 使用方式

首版至少提供命令行调用：

```text
qimen chart --datetime 2026-09-08T14:30:00+08:00
qimen chart --datetime 2026-09-08T14:30:00+08:00 --format json
qimen chart --now
qimen chart --now --longitude 121.47 --latitude 31.23
```

`--now` 未指定日期时间时读取程序运行设备的当前时刻；如果同时提供 `--datetime` 和 `--now`，应返回参数冲突错误。API 中可使用 `use_current_time: true` 代替显式 `datetime`。网页端提供“当前时间起盘”和“重新起盘”按钮，生成结果后固定本次取时瞬间。

后续提供以下接口：

```text
POST /api/chart
GET  /api/rules
GET  /api/chart/{id}
```

## 6. 非功能需求

- 相同输入与相同规则版本必须产生完全一致的结果。
- 核心排盘逻辑应使用纯函数或无状态服务，便于测试。
- 规则数据与界面、接口代码分离。
- 对无效时间、时区、经纬度和策略名称返回明确错误。
- 所有关键中间结果可追踪，包括交节时刻、三元、局数、旬首和值符值使落宫。
- 当前时间起盘必须记录取时瞬间、设备时区、用户时区、经纬度及时间模式。
- 中文显示与 JSON 字段编码统一使用 UTF-8。
- 计算引擎不依赖具体界面，可被命令行、API 和网页复用。

## 7. 建议程序结构

```text
qimen/
├── src/
│   └── qimen/
│       ├── calendar/
│       │   ├── lunar.py
│       │   ├── solar_terms.py
│       │   └── ganzhi.py
│       ├── rules/
│       │   ├── solar_terms.json
│       │   ├── ju_table.json
│       │   ├── palaces.json
│       │   └── sect_config.json
│       ├── engine/
│       │   ├── yuan.py
│       │   ├── earth_plate.py
│       │   ├── heaven_plate.py
│       │   ├── stars.py
│       │   ├── doors.py
│       │   ├── spirits.py
│       │   └── chart.py
│       ├── api/
│       └── cli.py
├── tests/
├── docs/
│   ├── 规则确认表.md
│   └── 标准排盘案例集.md
└── pyproject.toml
```

核心计算流程：

```text
输入时间与地点
  → 时间校正
  → 节气与四柱计算
  → 阴阳遁、三元及局数判定
  → 地盘排布
  → 旬首、值符和值使定位
  → 天盘、九星、八门和八神排布
  → 旬空与驿马计算
  → 九宫盘及 JSON 输出
```

## 8. 实施阶段

### 阶段一：规则考证与确认

- 收集可引用的古籍、现代教材和公开排盘案例。
- 建立二十四节气阴阳遁及三元局数表。
- 锁定拆补法、中宫寄宫、换日和值使运行口径。
- 编写《规则确认表》和《标准排盘案例集》。

### 阶段二：历法与核心引擎

- 实现或集成可靠的干支、农历和节气计算。
- 实现阴阳遁、三元及局数判定。
- 实现地盘、天盘、九星、八门、八神排布。
- 实现旬首、旬空和驿马计算。

### 阶段三：命令行版本

- 支持时间、地点及规则选项。
- 输出文本九宫盘和 JSON。
- 输出关键计算过程。

### 阶段四：网页与 API

- 提供日期、时间、地点和规则选择控件。
- 展示完整九宫盘及计算依据。
- 支持 JSON 导出和排盘结果保存。

### 阶段五：校验与发布

- 逐项完成标准案例验证。
- 对照至少两套可靠资料或排盘结果。
- 固化规则版本并编写使用说明。

## 9. 测试与验收

### 9.1 标准案例

至少覆盖：

1. 冬至和夏至交节前后。
2. 普通节气交接前后。
3. 交节发生在日间的拆分案例。
4. 子时、午时和亥时。
5. 六种不同旬首。
6. 阴遁与阳遁各不少于五例。
7. 上元、中元、下元各不少于三例。
8. 中宫寄宫及天禽星案例。
9. 标准时间与真太阳时跨时辰案例。
10. 两种换日规则产生不同结果的边界案例。
11. `--now` 或 `use_current_time: true` 与同一秒显式 `datetime` 的等价案例。
12. 当前时间起盘后等待一段时间，已生成的盘不得自行变化；点击重新起盘才产生新取时瞬间。
13. 覆盖六旬旬空、日支/时支马星及空亡与马星同宫案例。

每个案例逐宫核验：局数、地盘干、天盘干、九星、八门、八神、值符、值使、旬空和驿马。

### 9.2 性质测试

- 三奇六仪的数量及分布正确。
- 八门、九星和八神无意外重复或遗漏。
- 相同输入重复计算结果一致。
- 节气交接前后按预期切换规则。
- 阴遁和阳遁的顺逆方向正确。
- 非法日期、经纬度和缺失参数得到明确错误。

### 9.3 首版验收标准

1. 输入合法时间与地点可生成完整九宫盘。
2. 结果明确显示时间模式、四柱、节气、阴阳遁、三元及局数。
3. 九宫盘包含地盘干、天盘干、九星、八门和八神。
4. 值符、值使、旬首、旬空和驿马计算完整。
5. 规则差异均有配置项或明确说明。
6. 标准案例与人工确认结果一致。
7. 命令行或 API 至少一种调用方式可稳定使用。
8. `--now` 或 `use_current_time` 可以完成当前时间一键起盘。
9. 当前时间结果包含取时瞬间和时区信息，且与同一时刻显式输入结果一致。
10. 文本与 JSON 均包含旬空和马星，九宫对应位置有空亡/马星标记。
11. 规则、源码、测试、案例和使用文档齐全。

## 10. 主要风险

- 不同资料对拆补法三元、值使起法及中宫寄宫的记载可能不一致。
- 日柱换日规则会影响临界时段的日柱和时柱。
- 真太阳时可能导致时辰甚至日期边界发生变化。
- 不同历法库的节气交接时刻可能存在秒级或分钟级误差。
- 网络排盘工具通常不公开算法，不能作为唯一正确依据。

项目应通过“规则来源 + 固定版本 + 人工案例”控制以上风险。每张盘至少显示规则版本、时间模式、换日方式、三元策略和中宫寄宫策略。

## 11. 交付物

1. 《拆补法时家奇门起盘程序任务书》。
2. 《拆补法规则确认表》。
3. 《标准排盘案例集》。
4. 核心计算引擎源码。
5. 命令行程序及结构化 JSON 输出。
6. 自动化测试与测试报告。
7. 安装、使用和规则版本说明。
